# Password Generator

A console password generator written in Python.

## Features

- generates one or more passwords
- allows choosing password length
- supports digits
- supports lowercase and uppercase letters
- supports punctuation
- uses the secrets module for secure random choice

## Run

```bash
python main.py